import React from "react";
import Plain from "./plain";
import Sass from "./Sass";

const Styling = () => {
    return(
        <div>
            <Plain/>
            <Sass/>
        </div>
    )
}

export default Styling