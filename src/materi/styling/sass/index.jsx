import React from "react";
import './index.scss';

const Sass = () => {
    return(
        <div>
            <button className="btn">ini adalah button kedua menggunakan sass</button>
        </div>
    )
}

export default Sass